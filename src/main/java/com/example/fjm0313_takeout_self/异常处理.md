（一）异常：
1. 测试接口时返回的是[]而不是json字符串
排查：发现是Result类未加@Data注解，导致序列化为json的时候，无法调用Result的getter方法赋值
解决：使用@Data注解
2. NoSuchBeanDefinition异常
排查：这是bean对象未注册进容器的异常，通过报错信息得知是“com.example.fjm0313_takeout_self.mapper.EmployeeMapper”没有被注册进容器，检查发现是因为没有加@Mapper注解
解决：使用@Mapper注解
3. 明明登录成功，测试current接口的时候，却返回未登录提示信息
排查：这是由于setAttribute时，使用了user.getId(),而user只含有name和password两种信息，导致id为空，通过空id查出了null
解决：更换为实际数据库的用户id
4. cannot be inherited with different type arguments:
排查：这是由于ServiceImpl<>的第二个参数和后面实现的接口的参数类型不一致
解决：对齐
5. Cause: java.sql.SQLIntegrityConstraintViolationException: Duplicate entry '��������' for key 'dish.name'
; Duplicate entry '��������' for key 'dish.name'] with root cause
解析：重复插入字段
排查：插入的菜名已存在 
6. version = version + 1WHERE id = ?
解析：sql语法不对
排查：dishMapper的version + 1和WHERE之间没空格、
解决：加空格
7. 'dependencies.dependency.version' for org.springframework.boot:spring-boot-starter-webmvc:jar is missing. @ line 36, column 21
解析：springboot里面的parent模块的版本丢失
解决：优先下载parent模块
8. 在依据Redis版本下降spring boot版本后，突然间所有文件都报了java file is located outside of the module source root,so it won't be compiled错误
解析：其实不是因为文件位置的问题，而是因为springboot降级后，原先的mybatis-plus和spring boot的某些依赖标签不可用，因为这些标签在就版本中不可见，所以导致依赖全部失效
9. [ERROR] Failed to execute goal org.apache.maven.plugins:maven-compiler-plugin:3.11.0:compile (default-compile) on project Fjm0313_takeout_self: Resolution of annotationProcessorPath dependencies failed: version can neither be null, empty nor blank -> [Help 1]
解析：没法处理version，原因是build里面没有显式指定版本
解决：手动指定版本号version
10. /F:/IdeaProjects/Fjm0313_takeout_self/src/main/java/com/example/fjm0313_takeout_self/config/LoginInterceptor.java:[11,32] 程序包org.jspecify.annotations不存在
解析：旧版本里没有相应的注解包
解决：删除相应的包
11. [ERROR] /F:/IdeaProjects/Fjm0313_takeout_self/src/main/java/com/example/fjm0313_takeout_self/config/LoginInterceptor.java:[52,5] 方法不会覆盖或实现超类型的方法
解析：把人参数少填了
解决：删除的只是注解，没让你删参数
12. Remote host terminated the handshake
解析：阿里云镜像的SSL连接不稳定，应该是因为开了VPN的缘故
解决：关掉VPN重新下载
13. failed to transfer during a previous attempt ，This failure was cached in the local repository and resolution is not reattempted until the update interval of mirror has elapsed or updates are forced
解析：上一次数据传输就失败了，失败的结果被缓存了，在下一次强制更新前不会重新尝试下载
解决：删除缓存文件 
14. rmdir /s /q不被IDEA的本目录下的powershell语法解析，因此更换语法删除缓存
15. pom.xml全部通过，但是项目中的引用却依旧报红
解析：maven没重载，索引没建立起来
启动maven Reload all maven projects，解决
16. 降级后 IDEA 的 Maven 导入出了问题，没有正确识别 src/main/java 为源码目录，所有 Java 文件图标变成咖啡色。
如果一开始我知道是降级的问题，我觉得只需要删除旧依赖并引入新依赖重新导入一下项目就行
17. Caused by: java.lang.SecurityException: Attempt to deserialize unauthorized class com.example.fjm0313_takeout_self.common.MQ.message.SeckillMessage; add allowed class name patterns to the message converter or, if you trust the message originator, set environment variable 'SPRING_AMQP_DESERIALIZATION_TRUST_ALL' or system property 'spring.amqp.deserialization.trust.all' to true
解析：这是RabbitMQ的反序列化安全机制，spring AMOP默认不允许反序列化任意Java对象
解决：往RabbitMQConfig类中加入一段代码：
@Bean
public MessageConverter messageConverter() {
return new Jackson2JsonMessageConverter();
}
18. Caused by: org.springframework.messaging.converter.MessageConversionException: Cannot convert from [[B] to [com.example.fjm0313_takeout_self.common.MQ.message.SeckillMessage] for GenericMessage [payload=byte[216], headers={amqp_receivedDeliveryMode=PERSISTENT, amqp_receivedRoutingKey=seckill.order, amqp_receivedExchange=seckill.exchange, amqp_deliveryTag=1, amqp_consumerQueue=seckill.order.queue, amqp_redelivered=true, id=2d8e94fe-6522-4e05-00d2-61b5ff523b1b, amqp_consumerTag=amq.ctag-aDGXig-uRhwKHjWmWz3LcA, amqp_lastInBatch=false, contentType=application/x-java-serialized-object, timestamp=1775818331761}]
解析：queue里面还存在Java原生序列化下留下的残留消息
解决：删除掉queue中的残余消息
19. 为什么会{
    "timestamp": "2026-04-12T10:16:09.041+00:00",
    "status": 404,
    "error": "Not Found",
    "path": "/seller/seckill/"
    }
因为create接口前面有问号，一旦create前面有问号就不会被识别为路径的一部分了，反而会被当成查询参数
20. 当成查询参数能细讲一下吗？

21. inequivalent arg 'x-message-ttl' for queue 'order.dlx.queue':
    received the value '30000' of type 'signedint' but current is none
解析：针对order.dlx.queue的不合理参数，就是说声明了order.dlx.queue的方法里面并没有声明x-message-ttl这个参数，但是在别处强行给他赋值了
排查：OrderDelayQueue写错名字了
22. org.springframework.amqp.rabbit.listener.BlockingQueueConsumer$DeclarationException: Failed to declare queue(s):[order.notify.exchange]
解析：声明队列失败，将交换机声明到了队列的位置上
排查：RabbitListener的queues写错了，写成exchange了
23. 出不来consumer的消息，但是出的来sender的消息
解析：这是因为sender的convertAndSend方法写错了，第二个参数不应该写成QUEUE的名字，应该写成路由键，不然queue接收不到消息
24. This failure was cached in the local repository and resolution is not reattempted until the update interval of mirror has elapsed or updates are forced.
解析：这是引入依赖时常见错误，表示由于中途的网络问题，导致下载失败，失败的缓存留在电脑里，需要手动强制删除
25. com.mongodb.MongoQueryException: Command failed with error 2 (BadValue): 'Field 'locale' is invalid in: { locale: "review" }' on server localhost:27017. The full response is {"ok": 0.0, "errmsg": "Field 'locale' is invalid in: { locale: \"review\" }", "code": 2, "codeName": "BadValue"}
解析：无法识别locale:review，因为Locale是语言规则/地区环境
排查：将collection写成collation，collation是排序规则的意思
解决：修改collation为collection。
26. 在抽出公共模块步骤时，下单时出现userId为空的情况，但是明明已经登陆过了，而且先前测试的时候也没有出现过这种情况
解析：我认为是UserContext没有迁移干净，同一个项目里使用了不同模块下的UserContext，这就导致userId无法同步，导致有些情况下的userId仍为空
解决：抽离干净公共类