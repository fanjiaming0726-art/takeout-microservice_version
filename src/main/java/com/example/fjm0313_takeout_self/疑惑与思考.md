思考
（一）项目内部
1. 为什么针对菜品的口味，份量需要单独再建一张表，一个新实体类，为什么不能直接一个dish表，一个dish实体类搞定所有问题
   答案：因为口味。份量这种字段属于是一对多的关系，如果在一列当中全部体现，看似比较方便，但不方便后续的提取，修改。所以针对新字段，可以新建一张菜品偏好表，用dish_id保持与原先菜品的联系，用id来区分到底是微辣还是中辣，是大份还是小份。如果没有dish_id的话，是不知道针对哪个菜品选口味的
2. HTTPServletRequest是根据前端传过来的字节流/json字符串映射到Tomcat服务器的吗？
   答案：是，HTTPServletRequest是Tomcat服务器根据前端传过来的请求信息字节流，将字节流信息打包成HTTPServletRequest的。我们需要用就在括号里面写参数，不需要就不写，但不管需不需要Tomcat服务器都会自动生成
3. User的logout方法是不是有问题，真实情况下肯定有多个用户登录，单个用户移除了userId，对于其他用户不会产生影响吗？
   答案：不会，因为每一个用户获得的session是独立的
4. 为什么使用double做加减运算会出现精度问题？
   答案：？
5. 我感觉其实setDefault的/default接口其实没有必要在代码中手动设置isDefault值的，因为从前端提交过来的数据中应该是把isDefault字段设置成1的
6. 为什么AddressBookController的每个方法都没有设置登录拦截？
   答案：这就是设置统一拦截器的好处了，因为逻辑其实都差不多
7. GET和POST，DELETE这些请求方式到底有什么区别，感觉如果前端提交的是DELETE，我在后端的DELETE接口里写get方法，那我实际上和GET所干的事情没有区别？
   答案：其实不同的请求方式的不同就在于提交参数的方式不同，GET是直接把参数放URL里面，其他的都是放body里面。
   这样的结果就是：
   （1）通过GET提交后，返回来的结果会被浏览器缓存，下次再访问同样的网址的时候就会直接把缓存中的结果返回给你，不会经过浏览器。而POST这些不会缓存，会直接打到服务器上
   （2）想要访问一个资源，把GET的URL直接复制再发过去就行，但是POST这些如果你复制URL之后它是不会起到和原来一样的效果的，因为body没了
8. 为什么update有update()，updateById()两种方法，而save却不需要划分
   答案：因为update有两种更新方式，根据wrapper匹配，根据实体的id匹配。而save不需要，只需要知道插谁就行了
   8.1 update()和updateById()有什么优劣？
   答案：update()可以一次对一个列表进行修改，而ById需要一个一个修改。更为便利点。
9. 为什么方法明确要求返回的是Result<AddrressBook>，可是我返回Result.file(String)类型的数据也可以，为什么不报错？
   答案：其实实际上是返回了Result<AddressBook>类型的，只是说object那一栏的值为null而已。如果是Result.success(String)，那么object的类型就会变为String，此时就会与AddressBook起类型冲突
10. 为什么给订单赋入地址的时候，不直接赋入一个addressBook，反而把字段设置成String类型并且给出一大段字符串拼接逻辑
    答案：因为addressBook和addressBookId相绑定，如果后期更改了id对应的地址，那么查询往期地址的时候就会查成更新后的那个地址，就搞错了
11. 为什么需要给地址字符串设置三元表达式
    答案：因为字符串为null时与其他字符串拼接会把null这个单词也拼进去
12. 就service.getById()/save/updateById()/list()方法，底层是怎么实现的？谁来实现？
13. Result怎么序列化的，谁来序列化
    默认就是Jackson，把Result序列化为json字符串
14. IService，BaseMapper，ServiceImpl之间的关系是IService通过被赋入ServiceImpl来调用BaseMapper
15. yml文件不空格会有什么影响？
    键值粘在一块，无法把值赋入键中
16. Orders，OrdersDetail,AddressBook，shoppingCart到底是什么关系？
    用户最终的orders订单是由一个个的shoppingCart和一个AddressBook对象组成的，组成完后被支付最后塞入订单明细OrderDetail，最后再把所有的shoppingCart删掉（ordersDetail一一对应shoppingCart）
    PS：我认为这个项目最难的就是用户从选菜到下单支付这个流程的实体对象的设计，因为各个实体之间具有强相关性，类与类之间的字段可能高度重合但互有区分
17. 为什么返回去的结果是List<OrdersDTO>而不是List<OrdersVO>，而且要把Dto设计成继承自Orders对象，要返回订单是咋样的直接只返回List<OrdersDetial>不就完了，返回什么DTO啊？
    DTO确实可以改为VO；Orders订单存的是一个订单的附加信息，例如订单号，收货人等，而OrdersDetail是订单当中的核心内容，二者缺一不可
18. 为什么优化结构后的selectList的括号参数为null?
    这样写的意思是不加条件的查询，也就会返回所有值，对于那种需要返回所有值的方法而言，就应该这样做
19. 为什么address book的update会被优化为clearDefaultByUserId？
    因为update实际业务作用就是把默认地址设为0，所以就是清空默认地址的意思
20. 为什么每次用户请求结束的时候都需要显式销毁userId?
    因为ThreadLocal的工作流程是这样的，Tomcat在接收用户的请求的时候会分配线程池里面的一个线程，用户会在这个线程里面的ThreadLocal写入userId，如果不删除，下一个用户使用到的就是前一个用户的userId，数据就会串。
21. 为什么拦截器中的handler在第一步进行判断后不参与后续的操作？
    因为（a instance of B b）是Java16后引入的一个语法糖，一旦发现a确实是B的对象，那么就会把a转型为b，并让b参与后续的操作
22. 我这个项目中有AOP的动态代理吗？
    有的，当一个方法被@Transactional注解标记的时候，到时候调用这个方法的接口服务就是一个AOP代理对象，它负责来执行这个方法，并且在发生异常时回滚
23. 那我想问一下，是执行到这个方法的时候接口才会被代理对象注入，还是说在识别到这个@Transactional注解的时候就把接口注入进代理对象，往后所有方法都是这个代理对象在调用？
    是后者，早在服务启动时就注入了代理对象，往后其他的方法的执行对象也是这个代理对象
24. AOP的原理是什么？
    AOP的原理就是，被像@Transactional，@Before，@Around这样的注解标记完方法后，Spring在启动的时候就会检查这几个类，发现实现接口了就用JDK动态代理，发现没实现接口就用CGLIB代理，前者用接口实现来代理，后者用目标类子类作为代理，并被注入给这个类在别处的对象
    AOP的概念是如何的呢：就是把所有方法的通用需求都交割一个人来管理，这个管理的人就是JDK代理对象
25. 讲讲JDK动态代理和CGLIB动态代理? 
26. 为什么不给dish的findById加缓存 
27. 为什么获取userId不能直接写Long userid = (Long) params.get("userId");？
    因为userId被识别出来可能是个Integer，Integer是无法强转为Long类型的 
28. 为什么Integer无法强转为Long类型？
    因为强转只能发生在具有继承关系的类上，Integer和Long类间没有继承关系，所以不可以 
29. 为什么reverseRangeWithScores返回的结果是Set<ZSetOperations.TypedTuple<Object>>这么个奇怪的数据结构？
        Tuple这个数据结构不常见，但是它可以存储多种不同类型的值，在我们的项目中Tuple存储的就是value和Score数据 
30. 就是说我可以指定redisTemplate.opsForZSet().reverseRangeWithScores(RANKING_KEY, 0, n - 1);返回的是什么，如果我把Set<TypedTuple<Object>>变成Set<TypedTuple<String>>，返回的就是RANKING_KEY呗
    不是，这个方法返回的是在RANKING_KEY下的value+Score数据，你这样返回来的就是Value数据 
31. 为什么选取LinkedHashMap<>()
因为这个hashMap内部的键值对可以按照插入顺序排列，普通的hashMap会乱序 
32. 为什么返回的不是Value+Score的数据吗，为什么<>里面只填Value的类型
因为Tuple这个数据类型内部就两个数据，一个泛型的Value，一个Double的Score、，Score是不需要指定数据类型的，所以只需要显示指定正确Value的数据类型就行
33. @PostConstruct这个注解什么作用？
就是说在这个类被注册进Bean容器后，自动执行被这个注解标记的方法
34. 我注意到每一个serialVersionUID都是1L，如果到时候真的使用了Java内置的序列化，这样设置UID全都一样会不会导致分辨不出来该反序列化为哪个
不会，因为这个UID是为了匹配后校验版本号的，不是被用来区分哪个类是哪个类的
35. 为什么用LocalDateTime作为时间而不是什么其他的表示时间的类例如LocalDate
36. 我经常听到spring反射这个动作，spring服务启动的时候通过反射干嘛呢?
通过反射扫描注解，调用构造方法创建对象，从Bean容器里找实例注入
37. new ClassPathResource这个类代表resource包吗?
不是，这表示类资源路径，类资源路径就是target/classes，下面是项目包+resource包下的其他东西，说白了就是把Java包和resources包去了剩下的在组合到一个包下
38. switch的lambda表达式是有返回结果的是吧?
对，就是有返回结果
39. 我发现一个问题，就是Redis扣除了之后，数据库不会跟着同步，那数据一致性怎么保证？
这就是接下来RabbitMQ要解决的问题，Lua脚本预扣除后发MQ消息校准数据库，保证数据的一致性
40. 想解决数据库扛并发的问题，为什么不把数据库连接池扩容呢？
41. 我注意到SeckillServiceImpl里面有个PostConstruct方法是用来初始化Lua脚本执行器的，为什么不选在服务方法里面初始化？
        因为这会导致用户每次调用trySeckill方法都会执行一遍初始化，没必要，这个东西不会变，只需要项目启动的时候加载好就行 
42. 那为什么不使用静态代码块来完成这个过程？
    没有使用到spring的依赖注入的类时，是可行的，但如果需要使用到spring的依赖注入的类，就不行了。因为类加载时机发生在spring服务启动之前，那时候还没有依赖注入，对象还是空的，就会发生空指针异常\
43. init()方法可不可以是private类型的？
    可以的，因为Spring是依靠反射来执行方法，即使加了private方法Spring也依然可以调用的到这个方法
44. 为什么查地址要查默认地址而不是查所有地址？
因为默认地址才是真正要送给你的地方
45. 我觉得秒杀订单的逻辑不合理，即使addressBook为空依然可以插入秒杀订单，应该是为空就报异常返回
46. 为什么handleSeckillOrder需要抛异常？
因为如果调用数据库的那几步操作出了问题，虽然spring服务会日志报错，但是对于RabbitMQ来说，它会把这些消息重新放在队列里重新处理，然后陷入错误的死循环
47. 为什么把消息放队列里再运行会一直进入错误的死循环呢，是不是一些业务逻辑上的东西会这样，但是网络上的偶尔错误不会这样？
对的
48. 商家只需要把菜品加入秒杀活动就行了，为什么非得给商家一个加进缓存的接口，商家不必管到底底层是怎么运行的呀，因此加进缓存只是在create操作里顺带完成的
对，这样想是对的。在createActivity方法中将菜品入缓存（取消load接口，保留loadActivity方法）
49. 我这个项目里的RestFul API 在哪里体现？是几个注解的事情吗？
对，使用了例如Get/Post/Delete/PutMapping等HTTP方法，而不是只使用Post提交方式
50. 我想吧serialVersionUID这个玩意删了，又不用java自带的序列化方式还用它干嘛？
51. 我觉得每一个接口都有失败或者成功两种结果吧，所以只写success是不是有点问题？
52. 怎么能只返回20个搜索结果呢，不应该是用户往下滑，然后滑一页下一页进来吗
53. knife4j是怎么知道我要手动输入一个参数呢？
因为有个参数被注解了@RequestParam，knife4j知道要接受一个参数，所以就给出了一个输入框














（二）Redis
1. 为什么redisTemplate在更新缓存的时候都是用的delete方法而不是更新方法？
因为在并发状态下更新缓存可能会出现缓存一致性问题，Redis和MySQL不一样，Redis写数据的时候不加锁，导致请求写缓存的顺序可能和写数据库的顺序不一样，导致缓存数据被错误的数据覆盖掉。但delete方法不会出现这个问题。所以我选择在每一次查数据的时候没从数据库找见才写入缓存，而不是在数据更改的是时候写入缓存
2. 为什么滑动窗口需要使用ZSet，是为了使用它的根据时间范围删除旧缓存的功能吗？ZSet是什么结构，它是不是和Set一样是Redis的底层数据结构，目前我看数据结构都有在redisTemplate方法上有所体现，opsForValue使用的是什么数据结构？
    对，就是为了使用它的按照分数范围删除旧缓存的功能；ZSet是有序集合，根据score的分数进行排序；ZSet和Set、List、String、Hash结构并称Redis的底层常用数据结构，不那么核心的就是位图和Stream这种，只是听说过；opsForValue使用的就是String结构
3. 为什么滑动窗口比固定窗口好？
    因为设计出固定窗口就意味着你给了用户突破限流的机会，这个机会就在窗口即将过期的时候。所以导致那一瞬间的高并发，会对数据库的并发查询造成冲击
4. 现了Redis的session共享后，Redis session在本项目中的具体作用是什么？
    Redis Session会作为一个拦截器拦截一个动作，这个动作就是request.getSession.setAttribute("userId",userId)，这个动作原本会Tomcat内存里开辟出一块空间存储数据，但Redis会把这一步拦住，转而把数据存入redis中，之后的删除和获取操作全部通过Redis完成，确保多台服务器能够共享到用户session 
5. redisTemplate.opsForSet().add(BLACKLIST_KEY, userId.toString()); 为什么要toString()？
    是因为用的RedisTemplate是<String, Object>，反序列化的时候会把userId默认转型成Integer类型的数据，这样与之后校验的Long类型冲突 
6. 为什么RedisTemplate的opsForSet的这个结构删除的时候需要把key和值一块删了，而不是像delete那样只删key就行 
因为如果像delete那样删key会把set里面所有key全部删掉，必须得选key来删
7. 为什么Set<String> members = (Set<String>) (Set<?>)redisTemplate.opsForSet().members(BLACKLIST_KEY);这一步为什么不加(Set<?>)就是错的？
    因为这是Java的泛型安全设计，你可以把Object强转成String，但是不能把Set<Object>转换为Set<String>，因为它不能让一个Integer数据放进去，所以需要一个桥梁，叫Set<?>，Java允许任何的Set转成Set<?>也允许Set<?>转成任何的Set。
8. ZINCRBY是什么？
这是一个Redis底层命令，被Java包装为increment()，Z代表ZSet，INCR代表increment，BY代表加多少
9. 为什么秒杀用Lua脚本？
因为Lua在Redis里面是原子操作，适合高并发场景
10. 为什么乐观锁不适合高并发？
因为高并发情况下，乐观锁带来的失败会导致大量的重试，比如100次请求每次可能只成功1次，剩余的99次全部失败，下一次又是只成功1次，导致全部做完需要上千次的数据库操作，数据库压力比较大
11. 为什么Redis里面用Lua脚本来实现秒杀功能？
因为Redis执行Lua脚本是原子性的，导致
12. 既然是基于原子操作从而使用的Lua脚本，那么为什么不使用Redis内部自带的事务呢？
        因为Redis事务只是一堆命令集合，没法掺入更多的业务，而Lua脚本却可以掺入业务逻辑，例如根据什么返回值做出什么动作
13. 解释一下redisTemplate.execute(seckillScript, Arrays.asList(stockKey, usersKey), userId.toString() );这句代码各个参数的意思？
    seckillScript就是Lua脚本执行器，Arrays.asList(stockKey, usersKey)就是KEYS，stockKey是KEYS[1],usersKey就是KEYS[2],userId.toString()就是ARGV，并且是ARGV[1]，因为后续其实还可以加进去
14. Redis集群会把不同的key扔给不同的服务器吗？
    会，会根据CRC16算法计算key的哈希值，然后对16384取模运算，最后把这个key发给有槽对应这个数的服务器上
15. 为什么是16384？是默认的吗，可以修改吗
    因为Redis集群内默认有16384个槽位。是默认值且不可以修改
16. 如果Redis集群模式下两个key哈希后不在一个服务器上，那么就会直接报错，因为Redis不允许Lua脚本同时操作多个服务器上的key
17. 如何确保KEYS里面的key在同一个服务器上？
    集群模式下，应该使用Hash Tag,也就是大括号，只要确保大括号里面的内容一致，就会被分入用一个哈希槽中，从而落在同一个服务器中
18. Redis集群模式是什么？
    Redis集群就是一堆的Redis数据库堆一块了
19. 分别就Redis单机，哨兵，集群三个模式大概介绍一下概念，用处，情境，区别？
20. Lua脚本有回滚功能吗？
没有
21. 每次服务重启后，都会使用Redis，而我的Redis服务一直没关，所以每次Redis存数据的操作会不会导致数据重复？、
22. 为什么Orders的createOrders接口没用到？
23. 我觉得consumer的查秒杀活动的有无和检查库存是否为0是不必要的，因为在Lua脚本里已经有了查活动和检查库存的逻辑，能发送MQ消息说明已经提前在Redis里检查并扣好了库存。
是这样的，因此我们把consumer里的检查逻辑去掉
24. Redis扣库存的速度是不是要比MySQL扣库存的速度要快？
对的，Redis是在内存里面处理，MySQL需要操作磁盘。在实际情况中确实可能出现Redis已经扣到只剩5，而MySQL还剩100件，这就需要RabbitMQ消息作为中间处理，在后台慢慢处理数据，而快速把数据返回
25. 为什么要实行限流？
26. Redis目前没有支付功能，必须引入支付功能
27. 为什么sendToAllSellers方法里给data设置的是Object类型而不是message消息类型？
因为未来接收的不一定只是OrderNotifyMessage消息，也可能是其他的比如说订单取消消息
28. 为什么要加isOpen()来做判断检验，不是已经将连接异常和关闭的移除了吗？
不一定恰好这样，有可能还没来的及清理就已经拿到了这个session，因此加一道保险
29. 为什么加了static也没办法跨类调用？
因为那是在注解参数里，注解参数有个特点，里面的变量必须是编译期常量，所以还得加final









（三）MySQL
1. 并发在这里的一个体现是数据库的并发查询，你能不能结合我的项目给我讲一下同一个查询接口在被多个用户同时使用时，数据库和Tomcat服务器会做什么动作？
    Tomcat会给每个用户一个线程，这三个线程同时访问Redis读取数据，读不到数据就去读数据库，数据库读取数据采用的是MVCC（多版本并发控制），所以可以同时读取数据不用加锁，但是如果需要同时写数据，例如执行update语句时，MySQL底层会加行锁，保证数据不被同时更改，执行完后再放下一个线程进来 
2. 行锁是什么，表锁是什么？
    行锁就是只锁表中的一行数据，通常在只修改一行数据的时候加；表锁就是锁整个表，MySQL在修改表的时候其他线程都不能进来 
3. MySQL是如何实现底层的事务的？结合我那个OrdersServiceImpl的事务方法聊一下？
    通过三个东西，undo log、redo log、锁
    undo log：在执行SQL语句前把数据原本的样子写进去，比如如果是插入语句，那么undo log里面就是id，如果是更新语句，那么就是这个数据原本的字对应原本的值
    redo log：把要做的修改写进去，再去修改内存中的数据页，这样能保证修改实际数据的时候MySQL断了，恢复后也能从redo日志继续操作
    锁：对于UPDATE这种需要上锁的SQL语句，锁的释放时机不在语句执行完，而在于整个方法COMMIT之后才释放锁
    如果中间有一步失败，就倒序回滚到先前的状态 
4. 那行锁是只要执行UPDATE语句都会加还是得识别出来多个SQL同时到达才加？
    只要执行UPDATE语句就会加锁，不管有没有并发 
5. 为什么会有并发查询压力，严重并发会给什么带来什么问题？数据库被打崩是什么意思？连接池被耗尽算问题吗，连接池和连接本身又没坏？
    因为同时有很多的请求进来等待处理。会给用户体验带来问题，被打崩就是数据库和服务器没有办法处理空余出来的请求了，导致用户的请求界面一直转圈最后超时报错。连接池被耗尽不会对连接池本身造成影响，但是没有办法处理新的请求了。
    设想这个场景就明白了，MySQL允许20个查询连接，tomcat允许100个请求连接，当20个请求连接匹配20个查询链接，而20个查询连接没有查完时，后面的请求就拿不到数据库连接，就会一直排队等着，导致服务器资源无法被释放给新的请求，所以新的请求一直进不来，页面一直转圈最后超时报错 
6. undo log 和redo log文件只有一个吗
    不，都是具有多个 
7. 除了UPDATE语句，还有什么其他的语句是需要上锁的？
    INSERT/UPDATE/DELETE这种写的操作都需要 
8. SQL语句里面的这个Param参数是不是跟spring的那个param不同，那个是把参数映射为Java对象，而这个param是吧Java对象映射成SQL语句的对象
    对，就是这样没错
9. 系统限流是为了防止什么问题，不限流会发生什么问题？这里说的系统是什么意思，是一个Springboot服务还是硬件机器？还是数据库？
10. 为什么设计排行榜的菜品-数量的键值对的时候，AI选择把数量设置为score而不是value
因为榜单是需要排名的东西，可以类比为分数排名，所以就把数量设置为分数，所以反而会把菜名设置为value，前面设置一个RANKING_KEY
11. @Transactional注解是只针对MySQL的吗，如果使用了另一个数据库怎么办
不是只针对数据库的，如果换了基本没影响
12. 为了加乐观锁，特地设置一个version字段，不用这样吧，数据库本身不是就会有锁吗？
对的，当时这样做是为了加功能而引入这个功能的，实际上按照实际需求，这一步没必要。
13. 为什么SeckillActivityMapper里面要使用UPDATE的SQL语句，而不使用mybatis-plus自带的方法？
因为mybatis-plus的方法不能保证线程的安全性，如果想改数据，就得先查出来，设值，再更新，这三步不是原子操作。所以换用SQL语句更方便保证线程安全
14. CustomerController的订单状态失败得有防御措施啊，不能失败了就失败了啊？
15. @Update(("UPDATE seckill_activity SET total_stock = total_stock + 1 " +"WHERE id = #{activityId} AND total_stock > 0"))
    int restoreStock(@Param("activityId") Long activityId);
这个方法的@Param和#{activityId}是什么关系
Mybatis通过@param括号里面的参数去SQL语句中找与之相同的变量名
16. Orders order = ordersMapper.selectById(orderId);
    if (order == null) {
    return;
    }
    我觉得这个防御性编程是没有必要的吧，是在insert(orders)操作之后发的异步消息，怎么可能order为空呢
因为你无法确定15分钟后订单是否依然会存在，别忘了你的submitOrder是个事务，发送RabbitMQ消息后，后面一旦有异常抛出，整个操作回滚导致订单也会被取消掉，等15分钟后RabbitMQ去查那时候order真的就为空了
17. 我觉得秒杀订单和日常菜单的状态得一样都设置成5才可以。
18. 为什么handleOrderTimeout需要加@Transactional注解》？
因为这份操作包含了多张表的操作，不仅是订单表更是库存表，所以需要加@Transactional注解
19. 秒杀失败后不能说您已参与过秒杀不能再次秒杀，应该还要将其从Redis秒杀名单中剔除，而且那个逻辑应该这么写，不应该是在秒杀成功的时候就往名单里写，而是在支付成功后才往里写
20. 






（四）RabbitMQ
1. RabbitMQ 与 RocketMQ架构上有什么区别？
RabbitMQ : producer - exchange - queue - consumer
RocketMQ : producer - topic - consumer
2. consumer怎么知道producer消息是转发给谁的，这需要它们四个哪一个起作用？
producer指定一个exchange后，发一个queue的key给exchange，exchange去找哪一个queue有这个key，就把消息发给哪个queue，监听这个queue的consumer自然就知道消息了
3. exchange是不是有多个，每一个exchange都知道所有的queue吗？
的确有很多个，但是每个exchange都只认识自己绑定的queue，不认识其他exchange绑定的queue
4. 为什么明明写好了RabbitMQ的配置类，pom，application.yml。rabbitMQ的页面还是没有queue队列？
因为这是spring的懒加载机制，只有第一次使用到了queue的时候才会去创建queue。
5. 秒杀订单应该和正常的订单分离吧？
对，确实应该分离，已经新创建了一个seckillOrder实体类
6. RabbitMQ有几种交换机？
四种：按照匹配方式划分，有Direct/Headers/Fanout/TopicExchange四种，DirectExchange是精确匹配的那种
7. 为什么一个Bean方法里用了个参数，但是却说could not be autowired？
因为一个Bean方法里的参数，是需要在Spring容器里面的，如果这个参数是也是通过Bean获得的，那一定得是它自己而不是它的父类
8. 为什么会需要一个JacksonJson2MessageConverter呢？
因为这不同于SpringMVC返回给前端消息用HTTP，使用内置的Jackson序列化器进行Json转换，Rabbit MQ传输消息的时候走的是AMOP协议，如果不显示指定就会使用Spring AMOP来走Java原生序列化，因此得指定另一个
9. 我觉得JacksonJson2MessageConverter跟SpringMVC的内置的Jackson序列化器差不多，都是Jackson？
对，底层都是一个Jackson库
10. Jackson针对传输的消息是不是只有序列化和反序列化Json格式的作用？不同协议格式的消息流传的全过程是什么？
对的，Jackson只有Java对象 <-> Json字符串之间相互转换的作用，在不同的协议下，同样一份Json字符串，会被打包成不同的格式，根据不同的协议加上这样那样的请求头，最后一起被发到服务器上、
11. Jackson2JsonMessageConverter这个类，会转成字符串，并按照AMOP协议发过去？
不是，它只负责转成Json字符串，至于按照AMOP协议打包，就是RabbitMQTem【plate的事了
12. 削峰填谷是什么意思？这个项目里谁来削峰填谷？
削峰：将原本短时间内的高流量打低
填谷：在之后一段时间内按照合适数据库性能的速度，平稳的处理数据
Redis + Lua来削峰，RabbitMQ来填谷
13. 为什么想把SeckillMessage放进自己新建的message包下就反复弹出啥啥类的提示？
因为多处类引用了这个类，IDEA这是在提醒你改变这个类的引用路径，不然其他引用了这个类的地方会报错

（五）spring
1. 在spring后端项目中，当一个标记了@Transactional的方法开始执行后，spring本身会做什么动作
本质上不是这个对象直接调用这个方法，spring先是创建一个代理对象，然后事务拦截器开启事务，之后代理对象来执行这个方法
2. 我使用的是我自己写的实现类调用这个事务方法，还会生成代理对象吗？
会，跟这个没关系，当通过注入赋入实现类时，赋入的不是原始的实现类，而是经过包装后的代理对象






































